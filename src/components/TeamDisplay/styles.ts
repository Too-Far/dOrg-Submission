import { makeStyles } from '@material-ui/core';
export const useStyles = makeStyles({
    slider: {
        marginBottom: '3em',
    },
    text: {
        marginBottom: '0.5em',
    },
    card: {
        height: 300,
        backgroundColor: 'teal',
        color: 'white',
        borderRadius: '10px',
        padding: '2em',
    },
});
